# 작업 진행 기록

## 2026-07-06

- [정사원 · 23:09 JST · 완료한 일] 배포 실브라우저 QA의 현재 판정을 확정했다. Vercel 프로젝트 연결 자체는 PASS지만 검증 대상 URL이 저장소·보고서에 없고, 로컬 Vercel/GitHub 읽기 조회는 외부 연결 차단, 인앱 브라우저는 0개여서 정상 질문·DART 출처·빈 결과/필터 해제·429·503·60초 timeout·사용자 취소·모바일·다크·IME·키보드·접근성·라이트 대비를 실제 실행하지 못했다. `PHASE2_2_FRONTEND_QA.md`를 항목별 `FAIL(미검증)` 체크리스트로 갱신했으며 제품 코드·운영 설정·배포는 변경하지 않았다. · [다음 할 일] 검증 대상 Vercel URL을 기록하고 그 URL을 연 인앱 브라우저 탭을 연결한 뒤 전 항목을 재실행해 화면·Network 증거를 남긴다. 전 항목 PASS 전에는 작업완료로 판정하지 않는다.

- [오차장 · 23:08 JST · 완료한 일] 배포 후 인프라 실사를 마쳤다. Vercel 프로젝트 링크와 HEAD=`origin/main` `d609155`는 PASS, 로컬 환경변수 존재·확정값 일치와 소스·Git 이력 Secret 0건도 PASS다. 그러나 외부 DNS 차단으로 Production/Preview HTTP 200, Vercel 원격 env, WAF, GitHub Actions를 조회하지 못했고, 직전 코드 커밋에는 lockfile이 없어 quality의 `npm ci` 통과가 불가능하며 `.next/static`도 없어 번들 Secret 검사는 SKIP됐다. 최종 판정은 FAIL이며 결과를 `office-reports/2026-07-06_2308_배포 후 인프라 실사.md`에 기록했다. 진단 중 노출된 IP HMAC 키는 Git에는 없지만 교체 대상으로 표시했다. · [다음 할 일] 외부 연결 환경에서 HMAC 키를 로컬·Vercel 동시 교체하고 Vercel env/WAF/Production·Preview HTTP, GitHub quality, 새 번들 Secret 검사를 재실행한다.

- [강대리 · 23:01 JST · 완료한 일] 운영 Supabase 읽기 검증을 종료했다. 실제 `.env`의 HTTPS URL·Secret 존재를 값 비노출로 확인하고 service_role로 임베딩 실데이터 SELECT→저장 벡터 4인자 `match_filings` 자기검색을 시도했으나 운영 호스트 DNS 해석 실패로 HTTP 응답 전에 종료됐다. `SUPABASE_DB_URL`·Management Token·`psql`·연결 브라우저(0개)가 없어 `verify_schema.sql`, RLS·HNSW 카탈로그 검증도 실행하지 못했다. 운영 DB 변경은 0건이며 로컬 DB 계약 회귀만 3/3 PASS다. 결과를 `2026-07-06_2301_운영 Supabase 읽기 검증.md`에 기록하고 운영 정상 여부 미실증으로 검증 판정을 FAIL 처리했다. · [다음 할 일] 네트워크 가능한 환경에서 `verify_schema.sql` 최종 PASS와 service_role 실데이터 SELECT·4인자 RPC HTTP 200을 확보하면 PASS로 전환한다.

- [오차장 · 23:05 JST · 완료한 일] 배포 후 인프라 실사에 착수해 현재 HEAD `d609155`와 로컬 원격 추적 `origin/main`이 일치(앞/뒤 0)하고 Vercel 프로젝트 `web` 링크가 생성된 것을 확인했다. 반면 인앱 브라우저는 0개이고 셸·별도 런타임 모두 Vercel·GitHub·배포 후보 호스트 DNS가 `ENOTFOUND`라 Vercel 환경변수·배포 URL·WAF와 GitHub Actions를 원격 조회하지 못했다. 로컬에는 `web/package-lock.json`과 `web/.next/static`이 없어 CI 재현 및 번들 Secret 검사는 아직 PASS 처리할 수 없다. · [다음 할 일] 커밋·workflow·Secret 추적 상태를 정적으로 대조하고, 가능한 공개 경로를 한 번 더 확인한 뒤 항목별 PASS/FAIL 최종 보고서를 작성한다.

- [정사원 · 23:00 JST · 완료한 일] 배포 후 실브라우저 QA를 재개했다. 최신 단일 승인 보고서와 현재 저장소를 대조한 결과, 이전 보고 시점과 달리 HEAD가 `d609155`로 전진했고 `web/.vercel/project.json`·배포 커밋이 존재함을 확인했다. 다만 인앱 브라우저 연결 목록은 여전히 0개이며 문서에는 확정 배포 URL이 기록돼 있지 않다. · [다음 할 일] Vercel 프로젝트 메타데이터와 읽기 전용 배포 조회로 실제 URL을 확정한 뒤, 브라우저 연결이 확보되면 시나리오별 기능·접근성 QA를 실행한다.

- [강대리 · 22:59 JST · 완료한 일] 운영 Supabase 읽기 검증을 재개했다. 최신 DB 적용 보고서와 `db/schema.sql`, `db/verify_schema.sql`, 실제 웹·파이프라인의 Supabase 호출 경로를 대조해 검증 범위를 스키마 PASS, 4인자 RPC service_role 호출, RLS·HNSW 인덱스, 실데이터 임베딩 검색으로 고정했다. 운영 DB는 무변경 원칙을 유지한다. · [다음 할 일] 현재 환경의 DB/Data API 연결 가능 여부를 Secret 비노출 방식으로 확인하고 가능한 운영 검증을 실행한다.

- [최과장 · 22:09 JST · 완료한 일] Gemini 실측 원본 한도를 답변 10 RPM/20 RPD, 임베딩 100 RPM/1,000 RPD로 확정하고 80% 운영값을 답변 8 RPM/16 RPD, 임베딩 80 RPM/800 RPD로 정책·배포 문서·환경변수 예시·웹 README·백엔드 현행 보고서에 정합화했다. AI Studio 재확인·잠정값 표현을 현행 문서에서 제거하고, 답변 16 RPD가 재시도 포함 전 사용자 합산 상한이라 실사용 유입 시 무료 티어 병목이라는 caveat를 기록했다. 런타임은 모델별 값을 필수 환경변수로 fail-closed 처리하고 테스트 픽스처가 확정값을 전달함을 대조했으며, API·클라이언트·DB 회귀 26/26과 `git diff --check`가 통과했다. 과거 날짜형 보고서·역사 스냅샷은 당시 기록으로 보존했고 커밋·push·배포는 하지 않았다. · [다음 할 일] 배포 담당이 확정값을 Vercel 환경변수에 등록하고 Preview에서 실제 Upstash 한도·429 동작을 검증한다.

- [오차장 · 22:09 JST · 완료한 일] 루트 `.env`에 64바이트 랜덤 `RATE_LIMIT_IP_HASH_KEY`와 확정 모델 RPM(답변 8, 임베딩 80)을 값 노출 없이 추가하고 파일 권한을 600으로 고정했다. npm Registry·Vercel API는 모두 DNS 해석 실패였고 온라인 lockfile 생성은 90초 후 중단, 오프라인 생성은 `eslint` 캐시 부재 `ENOTCACHED`로 실패했다. 현재 회귀 26/26, 직접 TypeScript strict, 소스·Git 이력 Secret, 금지 `NEXT_PUBLIC_` 0건, `git diff --check`는 통과했다. · [다음 할 일] 외부 DNS가 허용되면 lockfile 생성부터 전체 빌드 게이트를 재실행하고, 모두 종료 0인 뒤 Vercel 프로젝트·환경변수·Preview·WAF를 적용한다. Production은 사장 승인 전까지 금지한다.

- [정사원 · 22:09 JST · 완료한 일] 최종 작업 트리를 **57개(추적 수정 7 + 미추적 50)**로 확정하고 영역별로 루트 4·`.github` 2·DB 2·문서 4·스크립트 3·웹 24·보고서 18개를 대조했다. 최신 단일 승인 문서에 중복·모순 보고서 처리 기준, 확정 Gemini 한도, Preview→사장 GO/STOP→Production 승인 순서를 반영했다. 프론트 QA 문서에는 정상·빈 결과·429·503·60초 timeout·사용자 취소·모바일·다크·IME·키보드·접근성의 실행 절차·합격 기준·증거 양식을 추가했다. 인앱 브라우저 0개와 Preview URL 부재로 실측은 미검증 유지다. `git diff --check`, Markdown 로컬 링크 25개, API·클라이언트·DB 회귀 **26/26**이 통과했다. · [다음 할 일] Vercel Preview URL과 연결된 브라우저가 준비되는 즉시 체크리스트 전 항목을 실행하고 증거를 기록한 뒤 사장님 GO/STOP 판단용 결과를 갱신한다.

- [강대리 · 22:06 JST · 완료한 일] 사용자의 운영 Supabase 직접 접근 승인 후 재시도했다. 인앱 브라우저 세션은 0개였고, CLI 프로젝트 링크·Management API Access Token·DB 연결 URL도 없어 SQL Editor/직접 Postgres DDL 경로를 확보하지 못했다. service_role로 `filings`와 4인자 `match_filings` Data API를 비파괴 요청했으나 두 요청 모두 운영 호스트 DNS 해석 전에 차단돼 Supabase 응답과 운영 변경은 없었다. DB 계약 회귀는 3/3 PASS, 대상 `git diff --check`는 통과했다. 결과를 `PHASE2_2_DB_APPLY_VERIFY.md`에 갱신했다. · [다음 할 일] 사용자가 로그인된 인앱 브라우저 탭을 연결하거나 `SUPABASE_DB_URL`/`SUPABASE_ACCESS_TOKEN` 중 하나를 제공하면 즉시 `schema.sql` 적용 → `verify_schema.sql`의 `PASS` 확보 → 4인자 RPC Data API 확인을 수행한다.

- [정사원 · 22:06 JST · 완료한 일] 과거 승인·QA 보고서와 현재 `git status --short --untracked-files=all`을 대조해 작업 트리를 **57개(추적 수정 7개 + 미추적 실제 파일 50개)**로 재집계했다. 기존 56개 기준 이후 `office-reports/PHASE2_2_DB_APPLY_VERIFY.md` 1개가 추가됐다. 문서상 핵심 모순은 운영 배포 완료라는 파일명, 과거 19/19·25/25 수치, AI Studio 재확인 필요 표현이며 최신 확정값·26/26·현재 배포 보류 판정과 분리해야 한다. 인앱 브라우저 연결 대상은 0개여서 Preview URL 실측은 시작할 수 없었다. · [다음 할 일] 파일 단위 영역 집계를 확정하고 최신 단일 승인 문서에 중복·모순 정리 노트와 Preview QA 실행 체크리스트·현 상태를 반영한 뒤 정적 검사를 재실행한다.

- [강대리 · 22:05 JST · 완료한 일] 최신 배포 승인서와 기존 운영 DB 적용 보고서, `db/schema.sql`, `db/verify_schema.sql`, 현재 환경변수 이름과 Supabase CLI/프로젝트 링크 상태를 재확인했다. 운영 DDL·카탈로그 검증에 필요한 DB URL/Access Token/로컬 프로젝트 링크는 없고, 보유값은 Data API용 URL·service_role 키뿐이다. SQL 계약 자체는 RLS, 4인자 RPC, 구버전 3인자 제거, 브라우저 역할 차단, service_role 권한, SECURITY INVOKER·빈 search_path를 모두 검사하도록 구성돼 있다. · [다음 할 일] 로그인된 Supabase 대시보드 접근 경로를 확인해 SQL Editor에서 schema 적용 후 verify 결과 `PASS`를 확보하고, Data API로 4인자 RPC 호출 권한도 비파괴 확인한다.

- [최과장 · 22:05 JST · 완료한 일] 최신 승인 문서와 Rate Limit 정책, 코드·테스트·환경변수 예시의 Gemini 한도 표기를 전수 검색했다. 코드·테스트는 답변 8 RPM/16 RPD, 임베딩 80 RPM/800 RPD로 일치하지만 정책·배포 문서·환경 예시·최신 검토 보고서에는 AI Studio 재확인 필요 또는 잠정값 표현이 남아 있음을 확인했다. · [다음 할 일] 전달받은 실측 원본값 10/20·100/1,000과 80% 운영값 8/16·80/800을 확정값으로 정리하고, 답변 모델 16 RPD가 전 사용자 합산 일일 상한이라는 병목을 명시한 뒤 정적 대조 검사를 수행한다.

- [강대리 · 09:05 JST · 완료한 일] Gemini 답변·임베딩 모델별 60초 RPM·최근 24시간 RPD 제한을 재시도마다 실제 전송 직전 단일 Upstash EVAL로 원자 차감하는 구현과 API 회귀 보강을 완료했다. RPM 80 초과 선차단, 60초·24시간 2-key 동일 hash tag, 80/800 인자, 3회 재시도별 차감을 검증했다. 전체 API·클라이언트·DB 회귀 26/26과 `git diff --check`가 통과했다. README·Rate Limit 정책·배포 체크리스트·백엔드 보고서에 필수 RPM 변수와 현재 계약을 반영했다. 커밋·push·운영 설정 변경은 하지 않았다. · [다음 할 일] npm Registry가 가능한 환경에서 lockfile·완전 의존성을 설치한 뒤 `tsc --noEmit`·lint·production build를 재검증하고, AI Studio 활성 한도로 8/16·80/800을 재확인한 후 운영 Upstash 동시성 검증을 수행한다.

- [오차장 · 09:03 JST · 완료한 일] 요청된 로컬 배포 게이트를 현재 환경에서 끝까지 재검증했다. npm Registry `ENOTFOUND`와 오프라인 `ENOTCACHED`로 lockfile 생성 실패, `npm ci`는 `EUSAGE`, 표준 lint/typecheck/build는 실행 파일 부재로 127이었다. 반면 최신 API·클라이언트·DB 회귀 25/25, TypeScript 직접 strict, Secret·문서·공백·셸 검사는 통과했다. `qa.sh --build`는 production build 부재로 실패 1건·경고 2건이며 `.next/static` 검사는 SKIP이다. 선행 게이트 미통과에 따라 GitHub quality CI, 운영 Supabase, Upstash/HMAC, Vercel 환경변수·배포·WAF와 커밋·push는 수행하지 않았다. · [다음 할 일] npm Registry 가능한 환경에서 lockfile 생성·diff 검토부터 재개해 전체 체인을 모두 종료 0으로 통과시킨다.

- [이대리 · 09:02 JST · 완료한 일] 담당 프론트엔드 결함 수정과 검증을 마쳤다. 전체 API·클라이언트·DB 회귀 24/24, TypeScript strict, `scripts/qa.sh` 실패 0건, 전체 `git diff --check`를 통과했고 `PHASE2_2_FRONTEND_STATIC_REVIEW.md`를 현재 구현·수치·변경 파일 기준으로 갱신했다. QA 경고 3건은 기존 개발 의존성·production build·로컬 API 미실행이며, 운영 URL과 실행 가능한 브라우저가 없어 실브라우저 상태 전이·접근성 실측은 통과 처리하지 않았다. 커밋·push·배포는 하지 않았다. · [다음 할 일] 정상 의존성 설치와 production build가 준비되면 운영 또는 mock 서버에서 429 백그라운드 복귀, timeout·사용자 취소, 네트워크 단절, 320~768px·라이트/다크·키보드·스크린리더를 실브라우저로 검증한다.

- [강대리 · 09:01 JST · 완료한 일] Gemini 모델별 60초 RPM·24시간 RPD를 단일 Upstash EVAL로 원자 판정하고 재시도 직전에 차감하는 런타임 구현을 검토했다. API 회귀 16/16은 통과했지만 기존 초과 테스트가 RPD 800 차단만 증명해, 임베딩 RPM 80 선차단·2개 Redis 키의 동일 클러스터 슬롯·80/800 인자 전달을 독립 검증하는 API 회귀를 추가했다. 재시도 테스트도 매 시도의 2-key EVAL·80/800 인자를 검증하도록 보강했다. · [다음 할 일] 추가된 회귀를 포함한 전체 API·DB·TypeScript 검증을 실행하고 백엔드 최종 보고서와 진행 기록을 완료 상태로 갱신한다.

- [오차장 · 09:01 JST · 완료한 일] 코드 결함 후속 구현과 테스트 계약을 대조해 Gemini RPM·RPD 카운터의 Redis 2키 계약에 맞도록 `web/tests/api-route.test.mjs`의 mock과 필수 RPM 환경변수·한도 검증을 보완하고, `docs/RATE_LIMIT_POLICY.md`의 글로벌 상한 12 표기를 확정값 8로 정정했다. API·클라이언트·DB 회귀 22/22와 직접 TypeScript strict는 통과했다. npm Registry는 DNS `ENOTFOUND`, 오프라인 lockfile 생성은 `eslint` 메타데이터 부재 `ENOTCACHED`로 재차 실패해 lockfile은 생성되지 않았다. · [다음 할 일] `npm ci`와 통합 QA의 정확한 실패 경계를 남기고, lockfile 선행 게이트 미통과에 따라 외부 운영 변경을 중단한다.

- [이대리 · 09:01 JST · 완료한 일] `AskPanel`의 429를 절대 deadline과 `Date.now()` 차이로 계산하고 `visibilitychange` 복귀 시 즉시 재동기화하도록 연결했으며, 요청별 `AbortController` 60초 timeout·사용자 취소 버튼·unmount 정리를 반영했다. Strict Mode mount 플래그 복원과 취소 사유 경쟁 조건도 보완했다. API가 반환한 메시지만 보존하고 timeout·사용자 취소·기타 transport 오류를 고정 한국어로 정규화했다. 라이트 muted 토큰을 `#667085`로 조정했고, 연결 계약·오류 분기·deadline 계산·실사용 배경 AA 대비 테스트 5/5 및 TypeScript strict·대상 공백 검사를 통과했다. · [다음 할 일] 전체 클라이언트/API/DB 회귀와 통합 정적 QA를 실행하고 담당 보고서를 최종 상태로 갱신한다.

- [최과장 · 09:00 JST · 완료한 일] `PROGRESS.md`, 최근 배포 전 검증 보고서, `docs/RATE_LIMIT_POLICY.md`를 대조해 두 모델의 전달 기준값과 80% 계산식은 기록돼 있지만 동일 AI Studio 활성 프로젝트의 독립 실측은 여전히 미완료임을 확인했다. Chrome 화면 접근은 로컬 승인에서 거부됐고 인앱 브라우저는 연결된 탭이 0개라, 공개 문서나 전달값을 활성 프로젝트 실측으로 오인해 확정하지 않았다. Secret 값은 조회·출력·기록하지 않았고 설정도 변경하지 않았다. · [다음 할 일] 사용자가 Chrome 화면 접근을 허용하거나 AI Studio에 로그인된 인앱 브라우저 탭을 연결하면 `https://aistudio.google.com/rate-limit`에서 현재 API 키 프로젝트를 선택해 `gemini-2.5-flash-lite`와 `gemini-embedding-001`의 RPM·RPD를 기록하고, 80% 운영값·재시도 포함 처리량 영향을 계산해 `docs/RATE_LIMIT_POLICY.md`와 별도 최종 보고서에 확정한다.

- [이대리 · 09:00 JST · 완료한 일] 최근 프론트엔드 정적 검토와 현재 담당 소스를 대조했다. deadline 기반 429 계산, `visibilitychange` 동기화, `AbortController` timeout·사용자 취소, 전송 오류 정규화, 클라이언트 헬퍼 테스트가 작업 트리에 일부 반영된 상태를 확인했다. 라이트 `--text-muted: #667085`는 실제 사용 배경 기준 4.64:1~4.97:1로 AA 4.5:1을 충족한다. React 개발 Strict Mode에서 mount 플래그가 cleanup 후 복원되지 않는 경계와 취소 직후 timeout 사유가 덮일 수 있는 경쟁 조건은 보완이 필요하다. · [다음 할 일] 요청 생명주기 경계를 수정하고, 컴포넌트 연결 계약과 contrast 회귀 테스트를 추가한 뒤 TypeScript·클라이언트/API/DB 회귀·정적 QA를 실행한다.

- [오차장 · 00:00 JST · 완료한 일] `PROGRESS.md`와 최신 인프라·프론트·백엔드·변경 검토 보고서 및 현재 Git 상태를 대조했다. 기존 Secret 검사 보완은 완료 상태로 확정해 재작업 대상에서 제외했고, 잔여 게이트를 모델별 재시도 포함 RPM 보호·프론트 3개 결함·lockfile 및 전체 빌드 검증·운영 적용 순으로 고정했다. 현재 작업 트리의 기존 수정과 미추적 파일은 보존하며 push는 금지한다. · [다음 할 일] 실제 `rate-limit`·Gemini·AskPanel·CSS·회귀 테스트 구현을 읽고 미반영 결함만 수정한다.

## 2026-07-05

- [정사원 · 2026-07-06 09:08 JST · 완료한 일] 최신 후속 수정분까지 포함해 API·클라이언트·DB 회귀 **22/22**, 직접 TypeScript strict, `git diff --check`, 셸 구문, Markdown 로컬 링크 23개 검사를 재실행해 모두 통과했다. 기존 보고의 19개 테스트는 최신 기준이 아니며, 모델별 RPM/RPD 원자 카운터와 429 경과시간 계산·클라이언트 timeout/취소/오류 정규화 회귀 3개가 추가됐다. 라이트 모드 `--text-muted`도 `#667085`로 변경된 것을 확인했다. · [다음 할 일] 정상 설치·lint·production build·통합 QA의 현재 실패 경계를 재확인하고, 문서의 누락된 RPM 환경변수와 과거 수치·완료 상태 모순을 정리한다.

- [정사원 · 2026-07-06 09:00 JST · 완료한 일] `git status --short --untracked-files=all` 기준 현재 작업 트리를 **55개(추적 수정 7개 + 미추적 실제 파일 48개)**로 재집계했다. 기존 53개 기준선 이후 2026-07-05 23:54에 `web/src/lib/client/ask-request.ts`와 `web/tests/client-ask.test.mjs` 2개가 추가되어 55개가 된 것으로 확인했다. 디렉터리 축약형 `git status --short`의 15개는 실제 파일 수가 아니며 `.DS_Store`·`tsconfig.tsbuildinfo`는 ignore 대상이라 집계에서 제외한다. · [다음 할 일] 55개 전체 변경을 목적·중복·모순·배포 포함 여부로 전수 검토하고, 최신 코드 결함 수정분의 회귀·빌드 가능 여부와 Preview·Production URL/브라우저 연결 상태를 검증한다.

- [강대리 · 23:40 JST · 완료한 일] 기존 Rate Limit 보고서와 현재 구현을 대조해, 사용자 질문 글로벌 8 RPM과 별개로 Gemini 실제 외부 호출은 모델별 24시간 카운터만 거치며 재시도 3회가 60초 RPM을 초과할 수 있음을 재확인했다. 기존 Upstash EVAL 원자 판정 구조와 API 오류 계약을 확인했고, 답변 8 RPM·임베딩 80 RPM 및 기존 16/800 RPD를 모델별 2개 창으로 한 번에 판정하는 구현 방향을 확정했다. · [다음 할 일] 모델별 60초+24시간 원자 카운터와 필수 환경변수를 구현하고, RPM 초과 선차단·재시도별 차감·EVAL 단일 호출 계약 회귀 테스트를 추가한다.

- [최과장 · 23:52 JST · 완료한 일] 기존 Rate Limit 정책·배포 전 검증 보고서를 대조해 현재 모델(`gemini-2.5-flash-lite`, `gemini-embedding-001`), 전달 기준값(10/20, 100/1,000), 80% 계산식과 AI Studio 실측 미완료 상태를 재확인했다. `computer-use` 지침을 확인했으며 Secret 값은 조회·출력하지 않는다. · [다음 할 일] 로그인된 브라우저에서 실제 활성 프로젝트와 두 모델의 RPM·RPD를 확인하고 80% 운영값·기존값 차이·비용 및 처리용량 영향을 계산해 정책 문서와 별도 보고서에 반영한다.

- [오차장 · 23:54 JST · 완료한 일] 기존 인프라 보고와 현재 작업 트리를 대조해 `scripts/check-secrets.sh`에 `mktemp` 실패 즉시 종료와 `web/.next/static` 별도 Secret 검사가 반영된 상태임을 확인했다. Node 20.17.0·npm 10.8.3 실행 환경과 `git diff --check` 통과도 확인했으며, push 금지 지시를 재확정했다. · [다음 할 일] 강제 `mktemp` 실패·정상 Secret 검사를 실행하고 npm Registry 연결 확인 후 lockfile 생성부터 전체 게이트를 순서대로 실행한다.

- [이대리 · 23:52 JST · 완료한 일] 기존 프론트엔드 정적 검토 보고서와 현재 `AskPanel`·전역 CSS·테스트 구성을 대조해 담당 결함 3건을 재현 가능한 코드 단위로 확정했다. 현재 카운트다운은 timer tick 차감 방식이고, 브라우저 fetch에는 timeout·취소가 없으며 transport 오류 원문이 노출되고, 라이트 `--text-muted`는 `#8b95a5`로 3.03:1이다. 기존 사용자 변경은 보존하고 커밋·push는 하지 않는다. · [다음 할 일] deadline 계산과 visibility 복귀 동기화, 요청별 AbortController·timeout·오류 정규화, AA 대비 토큰을 구현하고 클라이언트 회귀 테스트 및 TypeScript 검사를 실행한다.

- [정사원 · 23:52 JST · 완료한 일] 기존 운영·QA 보고서와 `git status --short --untracked-files=all`을 재대조해 현재 기준선을 **53개(추적 수정 7개 + 미추적 실제 파일 46개)**로 확정했다. 48·49·52개 집계는 각 시점의 파일 생성 차이와 디렉터리 축약 또는 `PROGRESS.md` 누락 때문에 현재값으로 사용할 수 없다. 운영 DB 직접 연결 URL·Supabase Access Token·로그인된 대시보드와 공개 배포 URL은 확인되지 않았고, `psql`은 없으며 Supabase CLI만 존재한다. · [다음 할 일] 53개 전체 목록과 집계 규칙을 최종 보고서에 고정하고, DB 정적 계약·가능한 비파괴 운영 조회 및 로컬 브라우저 실행 경로를 검증한다.

- [박주임 · 23:52 JST · 완료한 일] 기존 프론트엔드 QA·정적 검토 보고서와 현재 `ask-panel.tsx`·`globals.css`를 1차 대조했다. 라이트 대비, 429 카운트다운, client timeout·오류 정규화 결함이 현재 소스에도 남아 있으며 상태별 UI·IME·키보드·모바일 실측 승인 기준은 하나의 문서로 통합되지 않은 상태다. · [다음 할 일] 컴포넌트 전체 상태 전이와 색상 토큰·반응형·다크·reduced-motion 구현을 세부 검토하고, 수정 우선순위·수용 기준·브라우저 테스트 매트릭스를 최종 UI/UX 명세로 작성한다.

- [정사원 · 23:23 JST · 완료한 일] 기준 변경 49개를 목적·중복·누락·Secret·커밋 포함 여부로 전수 분류하고, 이후 추가된 백엔드·프론트 담당 보고서까지 `CHANGESET_REVIEW.md`에 통합했다. 기본 QA 실패 0건·경고 3건, API·DB 19/19, 직접 TypeScript strict, Secret·Markdown 링크·공백 검사를 통과했다. 환경 예시·정책·테스트의 8/16/800 정합성과 종목 필터 설계 상태를 바로잡고, Secret 검사 범위를 Upstash/HMAC 및 환경 예시로 확대했으며 두 QA 스크립트의 `mktemp` 실패를 exit 1로 검증했다. 최종 판정은 **CI 준비 미완료 / push 금지 유지**다. 신규 차단은 Gemini 재시도 시 모델별 RPM 보호 공백 1건과 프론트 카운트다운·client timeout·라이트 대비 결함 3건이며, 기존 lockfile·build·운영·브라우저 차단도 남아 있다. 커밋·push는 하지 않았다. · [다음 할 일] 위 코드 결함 4건을 수정·회귀 검증한 뒤 네트워크 가능한 환경에서 lockfile→npm ci→lint/typecheck/19개 회귀/build→`qa.sh --build`→번들 Secret 검사를 완료하고 제품 커밋과 과거 기록 커밋을 분리한다.

- [강대리 · 23:18 JST · 완료한 일] 백엔드·API·DB 기존 변경 검토를 완료했다. API 16건·DB 3건 회귀 19/19, TypeScript strict, `git diff --check`가 통과했고 schema↔verify의 4인자 RPC·RLS·역할별 권한 계약도 일치했다. 확정 전달값과 어긋난 환경 예시·테스트의 `12/800/800`을 `8/16/800`으로 정정하고 실제 Upstash EVAL 인자 검증을 기존 19개 안에 추가했다. 결과는 `PHASE2_2_BACKEND_CHANGE_REVIEW.md`에 기록했다. 운영 Supabase·Secret·배포·push는 건드리지 않았다. · [다음 할 일] Gemini 재시도까지 포함한 모델별 60초 원자 카운터를 보완한 뒤, AI Studio 원본 한도를 재대조하고 운영 Supabase schema→verify PASS 및 Upstash 실동시성 검증을 수행한다.

- [오차장 · 23:20 JST · 완료한 일] 요청된 로컬 검증 체인을 끝까지 재시도했다. npm Registry는 DNS `ENOTFOUND`, 오프라인 lockfile 생성은 `eslint` 캐시 부재 `ENOTCACHED`로 막혔고 `web/package-lock.json`은 생성되지 않아 diff 대상이 없다. 순차 실행 결과는 `npm ci` 1, lint/typecheck/build 각 127, `qa.sh --build` 1(회귀 19/19 통과, production build 부재 실패 1·경고 2)이다. `check-secrets.sh`의 `mktemp` fail-fast와 `.next/static` 검사를 구현·검증했고, 관련 기존 보고서 링크와 `INFRA_CICD_REPORT.md`를 현 상태에 맞게 갱신했다. Secret 실제 키 패턴은 미검출이며 커밋·push·Vercel·HMAC·WAF·배포는 수행하지 않았다. · [다음 할 일] npm Registry 접근 가능한 환경에서 lockfile 생성·diff 검토부터 재개하고 `npm ci`→lint→typecheck→build→`qa.sh --build` 및 실제 `.next/static` Secret 검사를 모두 종료 0으로 통과시킨다.

- [이대리 · 23:18 JST · 완료한 일] `web/src` 프론트엔드 정적 변경 검토를 완료해 `PHASE2_2_FRONTEND_STATIC_REVIEW.md`로 보고했다. 질문·종목 필터·예시 전체 검색·빈 결과 필터 해제 재검색·429 잠금·503·upstream timeout 오류 카드·IME·키보드·반응형·다크·접근성 속성은 코드상 연결되어 있다. TypeScript strict, API·DB 19/19, 대상 공백 검사를 통과했다. 코드 결함은 (1) 429 카운트다운의 background timer drift, (2) fetch transport 오류 원문 노출 및 client timeout 부재, (3) 라이트 모드 11px muted 텍스트 대비 3.03:1의 3건으로 분리했다. `web/src`는 검토 범위상 수정하지 않았고 커밋·push·배포도 하지 않았다. · [다음 할 일] 세 결함을 별도 수정한 뒤 정상 의존성·production build·운영 또는 mock 가능한 브라우저 환경에서 상태별 UI, 320~768px, 다크, IME, 키보드, VoiceOver, 200% 확대를 실측한다.

- [오차장 · 23:19 JST · 완료한 일] npm Registry DNS `ENOTFOUND` 후 `npm install --package-lock-only --offline`도 캐시의 `eslint` 메타데이터 부재로 `ENOTCACHED` 종료되어 lockfile을 생성하지 못했다. 임의 lockfile은 만들지 않았다. `scripts/check-secrets.sh`는 `mktemp` 실패 시 명시적으로 exit 1 하도록 보완하고, `web/.next/static`이 있으면 production 번들을 실제 키 패턴으로 별도 검사하도록 추가했다. 셸 구문, 강제 `mktemp` 실패(exit 1), 정상 Secret 검사(exit 0)를 확인했고 현재는 build 부재로 번들 검사가 명시적 SKIP이다. · [다음 할 일] 요청 순서대로 `npm ci`부터 실행해 설치 게이트를 확인하고, 가능한 후속 lint·typecheck·build·`qa.sh --build` 결과를 수집한다.

- [정사원 · 23:20 JST · 완료한 일] 신규 웹/API/DB 코드와 테스트를 검토하고 API·DB 회귀 **19/19**, 직접 TypeScript strict, `git diff --check`, 셸 구문을 재실행해 통과했다. `scripts/check-secrets.sh`에는 동시 담당 변경으로 `mktemp` fail-fast와 `.next/static` 검사가 반영된 것을 확인했다. 예시 환경값·Rate Limit 정책에는 확정값(글로벌 8, 답변 16, 임베딩 800)과 다른 12/800 표기가 남아 있고, 종목 필터 설계 문서에는 이미 구현된 항목이 미구현으로 남아 있다. · [다음 할 일] 확정 운영값과 구현 상태에 맞게 예시·정책·설계 문서를 수정하고, 전체 Secret/QA를 다시 실행한 뒤 49개 파일별 커밋 포함 판정을 최종 보고서로 작성한다.

- [이대리 · 23:16 JST · 완료한 일] 기존 프론트엔드 QA·종목 필터 설계 기록과 현재 `web/src`를 1차 대조했다. 질문 제출, 종목 필터와 `company` 요청, 예시 질문의 요청별 전체 검색, 필터 적용 빈 결과의 필터 해제 재검색, 429 `Retry-After` 카운트다운·재전송 잠금, Enter/Shift+Enter/IME 조합 방지, 결과 포커스 이동, 모바일·다크·reduced-motion 스타일이 소스에 존재함을 확인했다. · [다음 할 일] API의 503·timeout 오류 계약과 화면 분기를 대조하고, 상태 전이 경계 조건·접근성·CSS를 정적 검사 및 TypeScript 검사로 검증해 코드 결함과 브라우저 실측 항목을 분리한다.

- [오차장 · 23:16 JST · 완료한 일] 기존 인프라 보고서와 현재 작업 트리를 대조했다. `web/package-lock.json`·`web/.next`는 여전히 없고, `scripts/check-secrets.sh`는 `mktemp` 실패 시 즉시 종료를 보장하지 않으며 `.next/static`을 별도로 검사하지 않는 상태다. 이어 `npm ping`을 10초 timeout·재시도 없음으로 실행해 npm Registry DNS가 현재도 `ENOTFOUND`임을 확인했다. 기존 팀 변경은 보존하고 Vercel·HMAC·WAF·배포·push는 범위에서 제외했다. · [다음 할 일] npm 캐시만으로 lockfile 생성이 가능한지 확인하고, 불가능하면 정확한 차단 원인을 남긴 뒤 Secret 검사 스크립트를 보완한다.

- [정사원 · 23:16 JST · 완료한 일] 기존 통합·QA·인프라 보고서와 현재 `git status --short --untracked-files=all`을 대조했다. 현재 작업 트리는 기존 보고의 48개가 아니라 **49개(수정 추적 7개 + 미추적 42개)**이며, 차이는 `office-reports/`가 11개가 아니라 12개(`PROGRESS.md` 포함)인 데서 발생했다. 커밋·push는 수행하지 않았다. · [다음 할 일] 49개 파일의 실제 diff와 내용을 목적·중복·누락·Secret 위험·커밋 포함 여부 기준으로 전수 검토하고 코드·문서 간 모순을 식별한다.

- [강대리 · 23:16 JST · 완료한 일] 백엔드·API·DB 변경 검토를 재개해 기존 담당 보고서, 현재 Git 변경 범위, `db/schema.sql`·`db/verify_schema.sql`의 정적 계약을 1차 대조했다. 운영 Supabase 적용·push·배포는 범위에서 제외했다. · [다음 할 일] API/Rate Limit 구현과 환경변수 예시의 확정값 일치 여부를 코드 단위로 검토하고, 회귀 19개 및 SQL 정적 검증을 재실행해 결함과 운영 대기 항목을 확정한다.

- [정사원 · 22:34 JST · 완료한 일] 운영·브라우저·배포 상태와 로컬 검증을 최종 독립 대조해 `PHASE2_2_QA.md`에 최신 판정을 추가했다. API·DB 19/19, 직접 TypeScript strict, 정적 QA 실패 0건, Secret 소스·Git 이력, Actions YAML·셸, `git diff --check`는 통과했다. 반면 lockfile 부재로 `npm ci`가 `EUSAGE`, lint/typecheck/build가 실행 파일 부재 127, `qa.sh --build`가 build 실패 1건으로 종료됐고 `.next`가 없어 production 번들 Secret 검사는 미실시다. `quality.yml`은 미추적이라 현재 커밋의 CI 결과가 아니며, Vercel 연결도 없다. GitHub 인증 만료·DNS 차단·인앱 브라우저 미연결로 라이브 Actions/Pages 화면은 확인할 수 없었다. **배포 승인 보류를 확정**했고 커밋·push·배포는 하지 않았다. · [다음 할 일] 네트워크 가능한 환경에서 lockfile→클린 설치→전체 build QA→번들 Secret 검사를 통과한 뒤 운영 Supabase·Upstash·AI Studio 예산·Vercel WAF를 적용하고 실브라우저 및 GitHub CI 성공을 확인한다.

- [오차장 · 22:26 JST · 완료한 일] 실행 가능한 범위의 인프라 검증을 완료했다. API·DB 회귀 19/19와 TypeScript 직접 검사, Secret·Python·문서·공백 검사는 통과했다. `qa.sh --build`는 production build 부재로 실패 1건·경고 2건이며, lint/typecheck/build 표준 스크립트는 불완전 설치의 `.bin` 부재로 종료 127이다. Vercel CLI 인증 토큰 존재는 값 노출 없이 확인했지만 프로젝트 미연결·외부 API 응답 정지, Upstash/HMAC/모델별 예산값 부재로 env·WAF·배포 적용은 불가했다. 결과와 재개 조건을 `INFRA_CICD_REPORT.md`에 기록했으며 커밋·push·배포는 하지 않았다. · [다음 할 일] npm/Vercel DNS가 허용된 환경과 Vercel team/project, Upstash 자격값, AI Studio 활성 RPD를 확보한 뒤 lockfile→클린 설치→전체 build QA→번들 Secret 검사→Vercel env/WAF→실브라우저 QA를 완료한다.

- [강대리 · 22:25 JST · 완료한 일] service role로 운영 Data API의 `filings` SELECT와 4인자 `match_filings` RPC를 비파괴 호출했으나 두 요청 모두 운영 호스트 DNS 해석 실패로 HTTP `000`에서 종료됐다. `node --test web/tests/db-schema.test.mjs`는 3/3 통과했다. 현재 사실에 맞게 `PHASE2_2_DB_APPLY_VERIFY.md`를 갱신해 운영 반영 미수행, CLI는 실행 가능하나 Access Token·DB URL·로그인된 대시보드·네트워크 경로가 없어 라이브 `PASS`를 얻지 못했음을 기록했다. · [다음 할 일] 네트워크 가능한 세션에 `SUPABASE_DB_URL`을 안전하게 주입하고 `psql -v ON_ERROR_STOP=1`로 schema→verify를 실행하거나, 사용자가 로그인한 SQL Editor에서 두 파일을 순서대로 실행해 마지막 `PASS`를 확보한다.

- [정사원 · 22:27 JST · 완료한 일] 이전 permission 차단은 해소되어 API·DB 회귀 19/19, 직접 TypeScript strict, 통합 정적 QA 실패 0건·경고 3건, Secret 검사, `git diff --check`, 셸 문법을 재실행해 통과했다. 다만 온라인 lockfile 생성은 응답이 없어 중단했고 오프라인은 `eslint` 캐시 부재(`ENOTCACHED`)로 실패했다. 따라서 `npm ci`·ESLint·production build·번들 Secret 검사는 아직 불가하다. · [다음 할 일] GitHub Actions 실행 이력과 공개 배포 URL·Vercel 연결 상태를 독립 확인하고, 소스 Secret 수동 교차검사 및 변경 diff 검토 후 최종 QA 보고서에 승인 여부를 확정한다.

- [오차장 · 22:25 JST · 완료한 일] lockfile 생성을 실제 재시도했다. 온라인 `npm install --package-lock-only`은 90초 이상 레지스트리 응답 없이 대기해 중단했고, 5초 timeout의 `npm ping`으로 `registry.npmjs.org` DNS `ENOTFOUND`를 확정했다. 오프라인 생성도 npm 캐시에 `eslint` 메타데이터가 없어 `ENOTCACHED`로 실패했다. 실행 permission 병목은 해소됐지만 현재 세션의 외부 DNS 차단으로 lockfile·클린 설치가 막혀 있으며 임의 lockfile은 만들지 않는다. · [다음 할 일] 기존 로컬 모듈로 19개 회귀와 직접 TypeScript 검사를 실행하고, `npm ci`·lint·build의 정확한 실패 경계를 기록한 뒤 Secret·문서·셸 등 비네트워크 통합 QA를 완료한다.

- [최과장 · 22:22 JST · 완료한 일] 실제 설정에서 답변 모델 `gemini-2.5-flash-lite`, 임베딩 모델 `gemini-embedding-001`을 확인하고 2026-07-03 UTC 갱신된 Gemini 공식 Rate limits 문서를 재검증했다. 공식 문서는 대화형 고정 수치 대신 프로젝트별 AI Studio 활성 RPM·RPD 확인을 요구한다. 인앱 브라우저가 연결되지 않아 로그인 콘솔 실측은 수행하지 못했으며, 근거 없는 운영값 등록을 막기 위해 기존 `800/800`을 참고값으로 강등하고 `docs/RATE_LIMIT_POLICY.md`를 운영 수치 미확정·배포 차단 상태로 바로잡았다. · [다음 할 일] 로그인된 AI Studio의 `rate-limit` 화면에서 두 모델의 활성 RPM·RPD를 기록하고 `floor(RPD × 0.8)` 및 글로벌 RPM 식을 적용해 표와 운영 Secret을 확정한다.

- [강대리 · 22:24 JST · 완료한 일] 쓰기 가능한 임시 HOME으로 Supabase CLI를 재실행해 과거 permission 게이트는 해소됐지만 `SUPABASE_ACCESS_TOKEN` 부재로 프로젝트 조회가 거부됨을 확인했다. 연결된 인앱 브라우저도 없어 로그인된 대시보드 SQL Editor 경로를 확보하지 못했다. 별도 Supabase/Postgres 연동 도구도 제공되지 않는다. · [다음 할 일] 보유한 service role 자격으로 비파괴 Data API 호출을 수행해 운영 테이블·4인자 RPC의 현재 상태를 부분 실증하고, DDL·카탈로그 검증 불가 범위와 필요한 인증 정보를 보고서에 갱신한다.

- [이대리 · 22:22 JST · 완료한 일] 기존 프론트엔드 QA 보고서와 현재 배포 메타데이터를 대조하고 실브라우저 연결을 시도했다. 프로젝트에는 운영 URL·Vercel 연결 정보·`web/package-lock.json`·`web/.next`가 없고, 이번 세션의 인앱 브라우저 목록도 비어 있어 배포 서비스 화면을 열 수 없었다. 정적 확인을 실측 통과로 대체하지 않고 정상·빈 결과·429·503·timeout·종목 필터·320~768px·다크모드·IME·키보드·스크린리더 전 항목을 미검증으로 `PHASE2_2_FRONTEND_QA.md`에 기록했다. · [다음 할 일] 운영 URL과 인앱 브라우저가 준비된 세션에서 전 시나리오를 실제 조작하고 응답 상태·포커스·접근성 트리·뷰포트별 캡처를 문서에 추가한다.

- [강대리 · 22:22 JST · 완료한 일] 운영 DB 적용 담당으로 기존 `PHASE2_2_DB_APPLY_VERIFY.md`와 실제 `db/schema.sql`·`db/verify_schema.sql`을 재확인했다. 현재도 `psql`·`pg_isready`와 로컬 Supabase 프로젝트 링크는 없고, 루트 `.env`에는 Data API용 `SUPABASE_URL`·`SUPABASE_SECRET_KEY`만 있으며 DB 연결 URL·Access Token은 없다. Secret 값은 출력하지 않았다. · [다음 할 일] 현재 세션에서 Supabase CLI 인증 상태와 프로젝트 조회 가능 여부, 별도 Supabase 연동 도구 유무를 다시 확인해 운영 SQL 실행 경로를 확보한다.

- [정사원 · 22:22 JST · 완료한 일] 최근 운영·인프라 보고서와 현재 저장소 상태를 독립 대조했다. `web/package-lock.json`, `web/.next`, `web/.vercel`이 모두 없고, 추적 환경파일은 `.env.example`뿐이며 원격 `main`은 로컬 HEAD와 동일하다. 공개 배포 URL도 문서에 확정값이 없고 Git 작업 트리는 Phase 2-2 변경이 미커밋 상태다. · [다음 할 일] 현재 세션에서 npm 실행 가능 여부를 확인해 lockfile 생성→클린 설치→lint/typecheck/API·DB 테스트→production build→통합 QA·번들 Secret 검사를 재현하고, GitHub CI 및 실제 배포 상태를 교차 확인한다.

- [오차장 · 22:22 JST · 완료한 일] 최근 `PROGRESS.md`·`INFRA_CICD_REPORT.md`와 실제 작업 트리를 대조했다. 현재 세션은 npm·스크립트 실행이 가능하며, 선행 병목은 여전히 `web/package-lock.json` 부재다. `web/package.json`의 고정 의존성·검증 스크립트와 `qa.sh`의 build·번들 Secret 검사 범위를 확인했고 기존 사용자 변경은 보존한다. · [다음 할 일] lockfile을 생성·검토하고 클린 `npm ci` 후 lint·typecheck·19개 API/DB 회귀·production build를 순서대로 실행한다.

- [오차장 · 19:48 JST · 완료한 일] 네트워크 복구 지시에 따라 로컬 검증 체인(lockfile 생성→`npm ci`→lint→typecheck→`test:api`→build→`qa.sh --build`→번들 Secret grep)을 재개하려 했으나, **이 세션의 실행 permission mode가 `npm`·`node <스크립트>`·`bash <스크립트>` 등 코드 실행 명령을 전부 승인 대기 후 거부**해 체인을 실행하지 못했다(네트워크 아닌 harness 게이트 문제, `dangerouslyDisableSandbox`로도 우회 불가). permission이 허용한 읽기전용 검증은 통과: 소스 Secret 정적 검사 클린(`AIzaSy` 0건, `sb_secret_`·`eyJ...` 매치는 패턴 정의·접두사 검증뿐), `NEXT_PUBLIC_` Secret 노출 0건, 추적 env는 `.env.example`뿐, `.gitignore`가 `.env*`·`.next`·`node_modules`·`.vercel`·`*.pem/*.key` ignore, CI `quality.yml`은 `npm ci`(lockfile 필수) fail-closed 게이트라 **lockfile 부재 시 CI 자체 실패**임을 재확인. 현재 산출물: lockfile 없음, `node_modules` 불완전(`.bin` 없음), `.next` 없음이라 번들 grep 대상 부재. 결과·재개 순서를 `INFRA_CICD_REPORT.md`에 정리. 커밋·push·배포·운영 연결 미수행. · [다음 할 일] Bash가 `npm`·`node`·`bash` 스크립트를 승인 없이 실행할 수 있는 permission mode(명령 allow 등록 등)로 재실행해 lockfile 생성부터 `qa.sh --build`·`.next/static` Secret grep까지 종료 0으로 통과시킨다. 자격증명 없이도 이 구간은 완료 가능.

- [강대리 · 19:37 JST · 완료한 일] 운영 검증 SQL을 보강해 `service_role`의 `SELECT`·`INSERT`·`UPDATE`와 시퀀스 `USAGE`·`SELECT`를 각각 검사하고, `anon`·`authenticated`의 테이블 전체 권한 및 시퀀스 권한이 모두 차단됐는지 확인하도록 수정했다. Gemini timeout을 재시도 후 안전한 `502 UPSTREAM_ERROR`로 변환하는 API 회귀도 추가했으며 API·DB 테스트 19/19가 통과했다. · [다음 할 일] 통합 정적 QA와 diff·Secret 검사를 실행하고, 실환경 DNS·운영 인증·Upstash 설정 부재로 실행하지 못한 항목을 별도 최종 보고서에 명확히 기록한다.

- [정사원 · 19:36 JST · 완료한 일] 현재 소스로 통합 QA, API·DB 회귀, 직접 TypeScript, 셸·Actions YAML·공백 검사를 독립 재실행했다. `scripts/qa.sh` 실패 0건·경고 3건, API·DB 18/18, TypeScript strict, 셸·YAML·`git diff --check`가 통과했다. npm registry는 DNS `ENOTFOUND`, 오프라인 lockfile 생성은 `ENOTCACHED`라 정상 설치는 계속 차단됐다. · [다음 할 일] 보안·rate-limit·DB·UI 구현과 변경 diff를 코드 기준으로 독립 검토하고, production 번들 검사의 범위와 운영 연결 상태를 확인한다.

- [강대리 · 19:34 JST · 완료한 일] 기존 백엔드·Rate Limit 보고서와 `db/schema.sql`, `db/verify_schema.sql`을 대조하고 운영 연동 전제조건을 재점검했다. 루트 `.env`에는 Gemini·Supabase API 변수는 설정돼 있지만 DB 연결 URL과 Upstash·IP HMAC·글로벌/모델별 예산 변수는 없고, 로컬 Supabase 프로젝트 링크·Vercel 링크·`psql` 실행 파일도 없다. Secret 값은 출력하지 않았으며 `.env`가 Git ignore 대상임을 확인했다. · [다음 할 일] Supabase CLI 인증·네트워크 및 API 호스트 도달성을 안전하게 확인하고, 가능한 실제 호출 범위와 운영 스키마 적용 차단 여부를 확정한다.

- [정사원 · 19:34 JST · 완료한 일] 기존 `office-reports/`의 최종 통합·인프라 보고서와 현재 작업 트리를 대조했다. 직전 배포 보류 사유는 `web/package-lock.json`·production build·운영 Supabase/Upstash/Vercel 적용·실브라우저 QA 부재이며, 현재도 커밋·push·배포는 수행되지 않았다. · [다음 할 일] 네트워크와 설치 도구 상태를 재확인하고 lockfile 생성·검토, `npm ci` 기반 전체 QA와 production 번들 Secret 검사를 순서대로 재실행한다.

- [이대리 · 19:34 JST · 완료한 일] 기존 프론트엔드 QA 보고서와 현재 `AskPanel`·전역 스타일을 대조하고 production 브라우저 검증의 선행 조건을 재확인했다. 현재도 `web/package-lock.json`, Next.js·ESLint 실행 파일, `styled-jsx`가 없어 production 앱을 시작할 수 없다. 기존 사용자 변경은 보존하며 의존성 설치 가능 여부부터 다시 확인한다. · [다음 할 일] 정상 의존성을 설치해 lockfile을 확보하고 lint·typecheck·production build를 통과한 뒤 인앱 브라우저에서 모바일·다크모드·IME·키보드·접근성·종목 필터·빈 결과·429를 실측한다.

- [강대리 · 16:09 JST · 완료한 일] 담당 범위 최종 검증을 마쳤다. `scripts/qa.sh` 실패 0건(API·DB 18/18), TypeScript strict, Secret 검사, `git diff --check`가 통과했고 결과를 `office-reports/PHASE2_2_RATE_LIMIT_BACKEND.md`에 정리했다. 패키지 파일과 Git 원격은 변경하지 않았다. · [다음 할 일] 운영 담당이 Upstash·Vercel 환경값과 WAF를 등록하고, DB 담당이 운영 Supabase에 `db/schema.sql` 적용 후 `db/verify_schema.sql`을 실행한다. 정상 의존성 설치 뒤 lint·production build·실연동 QA가 통과되면 Phase 2-2 배포 게이트를 닫는다.

- [정사원 · 16:09 JST · 완료한 일] 최종 통합 기준으로 API·DB 테스트 18/18과 직접 TypeScript 검사, Secret·Python·문서·YAML·셸·공백 검사를 통과했다. rate-limit 설정 누락이 정책대로 fail-closed `503 RATE_LIMIT_UNAVAILABLE`을 반환하도록 Route와 회귀 테스트를 보완했다. `scripts/qa.sh --build`는 API 테스트까지 통과했으나 production build 부재로 실패 1건·경고 2건이다. 실제 Gemini와 Supabase 직접 호출은 외부 네트워크에서 `UpstreamError`로 종료됐고, 인앱 브라우저가 없어 UI 실측도 불가능했다. 결과와 남은 차단 사항, 배포 승인 보류 판정을 `PHASE2_2_QA.md`에 최종 기록했다. · [다음 할 일] 네트워크 가능한 환경에서 lockfile 생성·`npm ci`·lint·typecheck·build를 통과하고, Upstash·AI Studio 예산·Vercel WAF·운영 DB를 적용한 뒤 실제 API·브라우저·번들 Secret QA를 재수행한다.

- [오차장 · 16:09 JST · 완료한 일] 최신 글로벌·모델별 예산 rate-limit 구현까지 반영된 상태에서 정적 QA 실패 0건, API·DB 17/17, TypeScript 직접 검사, Secret·문서·YAML·셸·공백 검사를 통과했다. `RATE_LIMIT_IP_HASH_KEY`, 글로벌 RPM, Gemini 모델별 일일 예산을 환경변수 예시와 Vercel 표에 추가하고 `INFRA_CICD_REPORT.md`에 완료·차단·재개 순서를 확정했다. lockfile·production build·운영 DB/Upstash/Vercel·브라우저 검증이 없으므로 배포 승인은 보류했다. · [다음 할 일] 네트워크 가능한 환경에서 lockfile 생성과 `npm ci`·build를 통과한 뒤 DB 연결정보와 운영 rate-limit 값을 안전하게 주입해 스키마·실연동·브라우저 QA를 완료한다. 그 전에는 커밋·push·Vercel 배포하지 않는다.

- [강대리 · 16:08 JST · 완료한 일] 최종 확정 정책 변경분까지 서버 구현을 보완했다. IP 3개 창과 앱 전체 12회/60초 창을 단일 Upstash EVAL에서 원자 판정하고, Gemini 임베딩·답변 모델은 재시도를 포함한 실제 전송 직전마다 최근 24시간 예산을 별도 원자 차감한다. Vercel 보증 IP만 정규화하고 32자 이상 전용 HMAC 키로 비식별화하며, 설정·Redis 장애는 외부 호출 없이 fail-closed 처리한다. API·DB 회귀 17/17과 TypeScript strict 검사가 통과했다. · [다음 할 일] 담당 범위 최종 diff·Secret·공백 검사를 수행한다. 운영 환경에는 기존 Upstash 2개 외 `RATE_LIMIT_IP_HASH_KEY`, `RATE_LIMIT_GLOBAL_RPM`, `GEMINI_EMBEDDING_DAILY_BUDGET`, `GEMINI_ANSWER_DAILY_BUDGET` 등록이 필요하며 환경 예시·배포 문서 반영은 인프라 담당 범위다.

- [오차장 · 16:11 JST · 완료한 일] rate-limit 코드가 요구하는 서버 전용 `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`을 루트·웹 환경변수 예시와 Vercel 배포표, 웹 README에 반영했다. Upstash 정상·초과·장애 검증과 Vercel WAF 설정을 배포 게이트에 추가했으며 Secret 검사 실패 0건, Markdown 링크, YAML, 공백 검사를 통과했다. 실제 값은 없고 Vercel 프로젝트도 연결되지 않아 운영 등록은 수행하지 않았다. · [다음 할 일] 인프라 보고서에 완료·차단 항목을 확정하고 현재 QA 상태 기준으로 배포 승인 여부를 판정한다.

- [이대리 · 16:06 JST · 완료한 일] 담당 프론트엔드 구현 후 `node web/node_modules/typescript/lib/tsc.js -p web/tsconfig.json --noEmit`, `scripts/qa.sh`, `git diff --check`를 통과했다. 통합 API·DB 회귀는 15/15이며 429 응답의 `RATE_LIMITED`·`Retry-After` 계약과 화면 처리가 일치한다. 로컬 Next.js 기동은 기존 불완전 의존성의 `styled-jsx/package.json` 누락으로 실패해 브라우저 실측은 보류했다. · [다음 할 일] 정상 `npm install` 완료 후 lint·production build를 실행하고 브라우저에서 예시 필터 유지/전체 요청, 빈 결과 전체 재검색, 429 카운트다운 자동 해제, 320px·다크모드·키보드 포커스를 실측한다.

- [오차장 · 16:08 JST · 완료한 일] rate-limit 회귀 테스트가 보완된 최신 소스에서 `scripts/qa.sh` 실패 0건·API/DB 15/15와 직접 TypeScript 검사를 통과했다. `.github/workflows/quality.yml`의 lockfile 부재 시 `npm install` 우회를 제거해 웹 설치를 `npm ci` 전용 fail-closed 게이트로 전환했고 YAML·셸 문법과 `git diff --check`를 확인했다. · [다음 할 일] lockfile 생성 차단과 운영 연결정보 부재를 보고서에 반영하고, 배포 승인 조건이 충족되지 않았으므로 커밋·push·Vercel 배포를 보류한다.

- [최과장 · 16:04 JST · 완료한 일] 공식 쿼터 검증 결과를 `docs/RATE_LIMIT_POLICY.md`에 반영해 Phase 2-2 운영 정책을 확정했다. IP 6회/60초·60회/시간·200회/24시간, 글로벌 12회/60초, 모델별 실제 호출 예산은 AI Studio 활성 RPD의 80%로 정했고, Upstash 원자 제한을 주 방어선·Vercel Hobby WAF 60회/10분을 보조선으로 명시했다. Upstash Free 월 50만 command에 맞춰 Analytics 비활성화, IP HMAC, fail-closed, 429 헤더·JSON, 배포 게이트도 확정했으며 문서 공백·로컬 링크 검사를 통과했다. · [다음 할 일] 구현 담당이 글로벌·실호출 예산과 오류 계약을 코드·테스트에 반영하고, 배포 담당이 AI Studio 활성 한도를 확인해 환경값과 WAF 규칙을 적용한다.

- [이대리 · 16:04 JST · 완료한 일] `ask-panel.tsx`에 예시 질문의 요청별 전체 검색(필터 입력값 유지), 필터 적용 빈 결과의 원인 안내·필터 해제 재검색, 429 `Retry-After` 파싱·카운트다운·전송 경로 잠금을 구현했다. `globals.css`에는 지우기 버튼 40×40 터치 영역, 빈 결과 액션, 카운트다운, 비활성·모바일·다크 상태 스타일을 반영했다. · [다음 할 일] 직접 TypeScript 검사와 가능한 정적 QA를 실행하고, 예시·빈 결과·429 상태 전이를 코드 기준으로 재점검한다.

- [오차장 · 16:06 JST · 완료한 일] 현재 소스로 `scripts/qa.sh`를 실행해 Secret·Python·문서·공백 검사는 통과했으나 API 회귀는 11건 중 6건 실패했다. 새 rate-limit 경로가 Upstash 설정을 먼저 요구하면서 기존 정상·입력 검증·외부 오류 테스트가 기대 상태 대신 500을 반환한다. rate-limit 구현은 동시 변경 중이므로 소스를 덮어쓰지 않고 테스트·환경 예시 정합성 완료를 기다려 재검증한다. · [다음 할 일] rate-limit 테스트와 환경변수 예시 반영 여부를 확인하고, lockfile 부재와 함께 최종 QA 차단 게이트로 관리한다.

- [정사원 · 16:10 JST · 완료한 일] 명령별 검증에서 `npm ci`는 lockfile 부재(`EUSAGE`), lint·정상 typecheck·build는 실행 파일 부재(127)로 차단됐다. 반면 직접 TypeScript 검사는 통과했고, rate limit 통합 직전 API·DB 테스트는 11/11 통과했다. 이후 새 rate-limit 코드가 Route에 들어오면서 통합 `scripts/qa.sh --build`의 API 테스트가 5/11만 통과해 회귀를 포착했다. 기존 테스트 환경에 Upstash 설정·허용 응답 모킹이 없어 모든 요청이 먼저 500으로 종료되는 것이 원인이다. · [다음 할 일] 새 rate-limit 테스트 계약이 통합되는지 확인하고 API·DB 회귀를 다시 통과시킨 뒤, 실연동·브라우저 검증 가능 범위를 판정한다.

- [오차장 · 16:03 JST · 완료한 일] 온라인 `npm install`은 registry 응답 없이 60초 정지해 중단했고, 오프라인 lockfile 생성도 npm 캐시에 `eslint` 메타데이터가 없어 `ENOTCACHED`로 실패했다. 불완전한 `node_modules`를 근거로 임의 lockfile을 만들지는 않았다. 루트 `.env`에는 Gemini·Supabase 런타임 변수는 있으나 DB 연결 URL은 없고, Supabase 프로젝트 설정과 Vercel 링크도 없어 운영 스키마 적용·환경변수 등록은 현재 불가능하다. Secret 값은 출력하지 않았고 `.env` ignore 및 비추적 상태를 재확인했다. · [다음 할 일] 작업 트리의 동시 변경을 보존하면서 가능한 정적 QA를 수행하고, lockfile 생성·운영 반영·배포 차단 조건을 최종 보고에 명확히 남긴다.

- [정사원 · 16:04 JST · 완료한 일] Node.js `v20.17.0`, npm `10.8.3`을 확인하고 lockfile 생성을 시도했다. 온라인 `npm install --package-lock-only`은 레지스트리 응답 없이 80초 이상 대기해 중단했고, 오프라인 재시도는 캐시에 ESLint 메타데이터가 없어 `ENOTCACHED`로 실패했다. `web/package-lock.json`은 생성되지 않았다. 루트 `.env`에는 웹 실연동 필수 변수명이 설정돼 있지만 값은 출력하지 않았고, 선택 설정은 서버 기본값 사용이 가능하다. · [다음 할 일] `npm ci`의 lockfile 차단을 명시적으로 확인한 뒤, 실행 가능한 API·DB 테스트와 직접 타입 검사, build·통합 QA를 모두 수행해 실패 경계를 확정한다.

- [최과장 · 16:02 JST · 완료한 일] 기존 rate-limit 정책과 실제 Q&A 호출 구조를 대조하고 Gemini·Supabase·Vercel·Upstash 공식 문서를 재검증했다. Gemini 최신 문서는 대화형 쿼터를 프로젝트별 AI Studio 조회값으로 전환했고, Vercel Hobby는 WAF rate-limit 1규칙을 지원하며, Upstash Free는 월 50만 명령으로 변경된 사실을 확인했다. · [다음 할 일] 검증된 쿼터와 공급자별 제약을 근거로 익명 IP·글로벌·실제 Gemini 호출 예산 수치와 구현 방식을 `docs/RATE_LIMIT_POLICY.md`에 확정한다.

- [이대리 · 16:00 JST · 완료한 일] `PROGRESS.md`, 최근 프론트엔드 QA 보고서, `docs/DESIGN_STOCK_FILTER.md`, `docs/RATE_LIMIT_POLICY.md`와 현재 `AskPanel`·전역 스타일을 대조했다. 예시 질문이 현재 필터를 그대로 전송하는 충돌, 필터 적용 빈 결과의 정정·전체 재검색 액션 부재, 28×28 지우기 터치 영역, 429 응답 카운트다운 미처리를 확인했다. · [다음 할 일] 예시 요청만 전체 검색으로 분리하고 빈 결과 재검색, 40×40 지우기 타깃, `Retry-After` 기반 전송 잠금·카운트다운을 두 프론트엔드 파일에 구현한다.

- [강대리 · 16:08 JST · 완료한 일] 확정된 `docs/RATE_LIMIT_POLICY.md`와 현재 API·DB 테스트 변경을 대조했다. IP당 6회/60초·60회/시간·200회/일 슬라이딩 윈도우, 429 본문·`Retry-After`/`RateLimit-*` 헤더 계약을 확인했다. 패키지 변경 없이 Upstash REST의 원자적 Lua 실행으로 서버리스 인스턴스 간 영속 제한을 구현하고, Vercel이 덮어쓰는 `x-vercel-forwarded-for`만 운영에서 신뢰하며 원본 IP는 HMAC 해시 키로만 저장하기로 확정했다. · [다음 할 일] rate-limit 서버 모듈과 Route 오류 계약을 구현하고 원자성·IP 스푸핑·429 헤더 회귀 테스트를 추가한다.

- [오차장 · 16:00 JST · 완료한 일] 최근 인프라·QA 보고서와 현재 작업 트리를 대조했다. `web/package-lock.json`은 아직 없고 quality CI가 lockfile 부재 시 `npm install`로 우회하며, 웹 소스의 외부 패키지 import는 현재 `next`, `react`, `server-only`, 테스트용 `typescript`로 `web/package.json` 선언과 일치한다. 실제 환경파일은 Git에서 제외되고 `web/.env.local`은 없으며, Supabase·Vercel CLI는 설치돼 있다. · [다음 할 일] `npm install`로 lockfile을 생성한 뒤 dependency tree와 lockfile 무결성을 검토하고 CI를 `npm ci` 전용으로 바꾼다.

- [정사원 · 16:00 JST · 완료한 일] 최근 Phase 2-2 종합·프론트엔드·백엔드 QA 보고서와 실제 파일을 대조했다. 종목 필터 구현은 현재 소스에 반영됐고 API·DB 테스트 파일도 추가됐지만 `web/package-lock.json`은 아직 없으며, 이전 보고서의 필터 누락 판정은 갱신이 필요하다. 루트 `.env`는 존재하므로 값은 출력하지 않고 필수 변수 이름과 실연동 가능 여부만 안전하게 확인한다. · [다음 할 일] 의존성 설치로 lockfile을 생성·검토한 뒤 `npm ci`, lint, typecheck, API·DB 테스트, production build, 통합 QA를 실행한다.

- [강대리 · 15:28 JST · 완료한 일] 기존 백엔드 QA 보고서와 `/api/ask`, Gemini·Supabase 서버 모듈, `db/schema.sql`, API 테스트를 대조했다. `company`는 API에서 공백 제거 후 RPC `filter_company`로 전달되고 DB에서 기업명 완전 일치로 검색되지만, 이 계약과 RLS 권한을 직접 고정하는 회귀 검증이 없음을 확인했다. 정상·빈 결과·외부 인증 오류 모킹은 기존 테스트에 존재한다. · [다음 할 일] company 전달·경계값 테스트와 DB 스키마 정적 계약 검사를 추가하고, 운영 키·DB 연결 정보 가용 여부를 확인해 가능한 범위의 실연동 검증을 수행한다.

- [정사원 · 15:28 JST · 완료한 일] 기존 Phase 2-2 QA·프론트엔드·백엔드·CI 보고서와 현재 Git 상태를 다시 대조했다. 이전 검증은 불완전한 `node_modules`와 lockfile 부재로 lint·build·브라우저·실서비스가 차단된 상태였고, 현재 소스에도 종목 필터 UI와 `company` 요청 연동이 아직 반영되지 않았음을 확인했다. · [다음 할 일] 의존성을 정상 설치해 lockfile을 생성하고 lint·typecheck·API 테스트·production build·통합 QA를 순서대로 실행한다.
- [오차장 · 15:28 JST · 완료한 일] 이전 인프라·QA 보고서와 현재 작업 트리를 재확인했다. `web/package-lock.json`이 없고 quality CI가 lockfile 부재 시 `npm install`로 우회하는 상태이며, 원격 저장소는 `origin/main`으로 연결돼 있다. 추적 중인 실제 환경변수 파일은 없고 `.env*`, `.vercel/`, 인증서 파일은 ignore 대상으로 유지된다. · [다음 할 일] 정상 네트워크에서 `web` 의존성을 설치해 lockfile을 생성하고 패키지 해시·스크립트·의존성 변경을 검토한다.

- [오차장 · 14:45 JST · 완료한 일] 기존 보고서 경로와 Git 작업 트리를 점검했다. `office-reports/`가 없어 이 기록 파일을 생성했고, 사용자 작업 중인 `.env.example`, `.gitignore`, `db/schema.sql`, `web/` 변경은 보존 대상으로 확인했다. GitHub Actions 2개, 환경변수 예시, ignore 규칙, QA 스크립트와 문서의 실행 경로를 대조했다. 추적 중인 실제 Secret 파일은 없으며 루트 `.env`와 `web/.env.local`은 ignore됨을 확인했다. · [다음 할 일] CI/배포/스크립트 구조를 정리하고 문서와 실행 명령을 같은 경로로 맞춘다.
- [오차장 · 14:53 JST · 완료한 일] 공통 `.gitignore`로 하위 환경파일·Next.js·Vercel·Python 산출물 규칙을 통합하고 `web/.gitignore` 중복을 제거했다. `scripts/qa.sh`와 `scripts/check-secrets.sh`, `.github/workflows/quality.yml`, `docs/DEPLOYMENT.md`를 추가했으며 Pages 설정과 Vercel `web` Root Directory, GitHub secrets/variables 문서를 맞췄다. `daily-briefing`은 데이터 변경 시에만 Pages 배포를 호출하도록 수정했다. Secret 검사, Python 구문, Markdown 링크, 이동 경로, API 테스트 7건은 실패 0건이다. · [다음 할 일] Actions/셸 문법과 전체 diff를 최종 점검하고, 네트워크 제한으로 남은 npm 설치·lint·build·실연동 항목을 명시한다.
- [오차장 · 14:55 JST · 완료한 일] 셸 문법, Actions YAML 3개, `git diff --check`, ignore 예외, 작업 트리·Git 이력 Secret 검사를 최종 통과했다. `scripts/qa.sh --build`는 API 테스트 7건까지 통과했으나 불완전한 `node_modules` 때문에 production build 단계에서 예상대로 실패했다. `npm install`은 네트워크 대기 후 중단했고 offline 설치도 `ENOTCACHED`로 실패했다. 결과는 `office-reports/INFRA_CICD_REPORT.md`에 기록했다. · [다음 할 일] 네트워크 가능한 환경에서 `npm --prefix web install`로 lockfile을 생성하고 `scripts/qa.sh --build`, 실제 API·브라우저 검증, 운영 DB 스키마 적용을 마친 뒤 Vercel을 연결한다.

- [강대리 · 14:46 JST · 완료한 일] Python 파이프라인·API Route·서버 모듈·DB 스키마·API 테스트와 모든 경로 참조를 조사했다. `pipeline/`, `db/schema.sql`, `web/tests/`는 현재 역할별 표준 위치라 유지하고, 비어 있는 `web/src/` 아래로 API 및 서버 모듈을 옮기는 최소 이동 범위를 확정했다. · [다음 할 일] `web/src/app/api`, `web/src/lib/server`, `web/src/lib/ask-types.ts`로 이동하고 테스트·문서 참조를 수정한 뒤 회귀 테스트를 실행한다.
- [정사원 · 14:48 JST · 완료한 일] 구조 이동 전 기준으로 API 모킹 테스트 7건, TypeScript 타입 검사, Python 바이트코드 검사, 셸 문법 검사, Secret 정적 검사를 통과시켰다. 현재 `web/node_modules`는 일부 의존성만 있는 불완전 상태라 ESLint 실행 파일이 없고 production build는 `styled-jsx` 누락으로 실행 불가함을 확인했다. `web/package-lock.json`도 현재 없다. · [다음 할 일] 영역별 이동이 끝나면 import·테스트 경로·문서 링크를 새 구조에 맞춰 재검사하고 CI/정적 QA 및 실환경 검증 체크리스트를 보강한다.
- [최과장 · 14:46 JST · 완료한 일] Git 추적·미추적·무시 파일, 용량, 경로 참조를 전수 대조해 이동 문서 4개, 빈 중복 소스 경로, 로컬 생성물, 의도적으로 추적하는 대시보드 데이터 산출물을 구분했다. 코드·구성 파일은 수정하지 않았다. · [다음 할 일] 구현 담당이 보고서 이동과 생성물 정리를 수행한 뒤 문서 링크, TypeScript alias, 테스트 import, Pages 경로를 전체 QA한다.
- [이대리 · 14:53 JST · 완료한 일] 프론트엔드의 `app` 페이지·레이아웃·전역 CSS와 `components`를 `web/src` 아래로 재배치했다. 백엔드 담당의 확정 이동 목록과 합쳐도 Next.js 앱 루트가 하나로 유지되도록 `tsconfig.json` 별칭을 `./src/*`로 수정하고 API 테스트 import와 `web/README.md` 경로를 맞췄다. 정적 자산은 없음을 확인했고 소스 경로의 `.DS_Store`를 제거했다. · [다음 할 일] 이동 후 lint, typecheck, API 테스트, production build를 실행하고 잔존 경로 참조를 점검한다.
- [이대리 · 14:56 JST · 완료한 일] 이동 후 API 모킹 회귀 테스트 7건을 모두 통과시켰고 새 import 경로가 런타임에서 해석됨을 확인했다. `web` 내부의 이전 `app/components/lib` 참조와 빈 디렉터리, `.DS_Store`, `tsconfig.tsbuildinfo`를 정리했다. lint·typecheck·production build는 `node_modules/.bin`이 없는 불완전 의존성 상태로 실행 시작 전 종료됨을 확인했다. · [다음 할 일] 네트워크 가능한 환경에서 `npm install` 후 `npm run lint`, `npm run typecheck`, `npm run build`를 재실행한다. 루트 기획 문서와 QA 스크립트의 이전 경로 참조는 해당 담당자가 새 `web/src` 경로로 갱신해야 한다.

- [강대리 · 14:54 JST · 완료한 일] `web/src/app/api/ask`, `web/src/lib/server`, `web/src/lib/ask-types.ts` 이동 결과와 테스트 import를 검증했다. API 모킹 회귀 테스트 7/7, Python compileall(`/tmp` 캐시), QA 셸 문법, Secret 정적 검사, DB RLS·RPC 권한 규칙, `git diff --check`가 통과했다. · [다음 할 일] 남은 설계 문서의 API·서버 모듈 경로를 새 `web/src` 구조로 수정하고 최종 잔존 참조를 검사한다.

- [강대리 · 14:48 JST · 완료한 일] 설계 문서의 API·공유 타입·서버 모듈 경로를 `web/src` 기준으로 수정하고, 기존 절대 경로 참조가 남지 않았음을 확인했다. 이동 후 TypeScript strict 검사, API 테스트 7/7, Python compileall, `git diff --check`를 최종 통과했다. · [다음 할 일] 백엔드 구조 정리는 완료했다. 네트워크 가능한 환경에서 정상 의존성 설치 후 lint·production build·실제 Gemini/Supabase 통합 검증을 수행한다.
- [정사원 · 14:53 JST · 완료한 일] 구현·QA 보고서 4개를 `office-reports/`로 모으고 루트·웹 README, 기획서, 보고서의 이동 경로와 현재 판정을 맞췄다. 통합 QA의 API Route 경로, 실행 권한, macOS Python 캐시 실패를 수정하고 Markdown 로컬 링크·이전 경로·공백 검사를 추가했다. 재실행 결과 API 테스트 7/7, TypeScript 직접 검사, Python 구문, Secret, Markdown 링크 10개, 이전 경로, YAML 문법, 셸 문법, `git diff --check`가 통과했다. · [다음 할 일] 최종 파일 목록과 리스크를 다시 대조하고 네트워크·운영 환경 체크리스트를 확정한다.
- [정사원 · 14:55 JST · 완료한 일] 전체 구조·문서·CI 최종 대조와 오프라인 QA를 완료했다. `scripts/qa.sh` 실패 0건·경고 3건, API 테스트 7/7, 워크플로 YAML 3개, 셸 문법, Markdown 링크, 이전 경로, `git diff --check`가 최종 통과했고 결과·변경 파일·완료 게이트를 `PHASE2_2_QA.md`에 확정했다. · [다음 할 일] 네트워크 가능한 환경에서 lockfile 생성, `npm ci`·lint·typecheck·build, GitHub quality 첫 실행, 운영 Supabase 스키마 적용, 실 API·브라우저·Vercel 검증을 순서대로 수행한다.

- [이대리 · 15:30 JST · 완료한 일] `AskPanel`에 선택형 종목명 입력 필터를 추가하고, 값이 있을 때만 기존 `/api/ask` 요청의 `company` 필드로 전송하도록 연결했다. 로딩 중 필터 잠금, 전체 종목 안내, 입력 지우기, 로딩·답변의 검색 범위 표시, 오류 재시도 시 직전 종목 조건 보존을 반영했다. 모바일·다크모드·키보드 포커스 스타일도 기존 UI 규칙에 맞춰 보완했다. · [다음 할 일] TypeScript·API 회귀·정적 QA를 실행하고 요청 본문 및 접근성 속성을 점검한다.

- [박주임 · 15:52 JST · 완료한 일] 종목 필터 UI/UX 설계안을 `docs/DESIGN_STOCK_FILTER.md`로 작성했다(코드 미수정). 배치(질문 카드 상단·구분선), 자유 입력 상호작용, 초기·로딩·빈 결과·오류·정상 상태, 모바일·다크모드·접근성, 디자인 토큰 매핑을 이대리 구현과 대조해 정리하고 FE 정합성 체크리스트를 붙였다. 백엔드 완전 일치(`filter_company`) 제약을 근거로 두 가지 보완을 우선순위 높음으로 지정했다: (1) 예시 질문 클릭 시 필터 종목 충돌로 조용히 빈 결과가 되는 문제(전체 검색으로 실행 권장), (2) 빈 결과에서 "필터가 원인일 수 있음" 정정 안내 + '필터 지우고 재검색' 액션 부재. 그 외 ✕ 클릭 후 입력 포커스 복귀, 지우기 버튼 터치 히트 영역 40×40, 다크 대비는 실측 대상으로 표기. · [다음 할 일] 이대리가 설계안 (1)(2) 보완을 반영하고, 브라우저 QA 시 다크·모바일·접근성 실측 항목을 확인한다.
- [오차장 · 22:05 JST · 완료한 일] 최종 배포 승인서·인프라 보고서·현재 Git/환경 상태를 대조했다. Node 20.17.0, npm 10.8.3, Vercel CLI 54.20.1 실행 가능 상태이며, 루트 `.env`에는 Upstash·Supabase 값과 확정 일일 예산이 있으나 `RATE_LIMIT_IP_HASH_KEY`와 두 모델 RPM 변수가 없음을 값 노출 없이 확인했다. `web/package-lock.json`, `web/.next/static`, Vercel 프로젝트 링크는 아직 없다. · [다음 할 일] HMAC 키와 확정 RPM 값을 로컬 환경에 안전하게 보완하고 lockfile 생성→클린 설치→전체 빌드 게이트를 실행한다.
