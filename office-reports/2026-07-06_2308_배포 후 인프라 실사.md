# 배포 후 인프라 실사 보고

작성: 오차장  
일시: 2026-07-06 23:08 JST

## 최종 판정

**FAIL — 작업완료 처리 불가.** 로컬 Git과 Vercel 프로젝트 연결은 확인됐지만, 외부 DNS
차단으로 실제 Production/Preview HTTP 200, Vercel 환경변수, WAF, GitHub Actions를 원격
검증하지 못했다. 또한 현재 커밋에는 `web/package-lock.json`이 없고 로컬에는
`web/.next/static`이 없어 CI와 production 번들 Secret 검사를 PASS 처리할 근거가 없다.

## 항목별 결과

| 점검 항목 | 결과 | 확인 근거 |
|---|---|---|
| Vercel 프로젝트 연결 | PASS | `web/.vercel/project.json`의 프로젝트명 `web` 확인. 파일은 Git ignore 상태 |
| Production URL HTTP 200 | FAIL | 배포 URL이 저장소·보고서에 없고 Vercel API 및 후보 호스트 DNS 조회 실패 |
| Preview URL HTTP 200 | FAIL | Preview URL 기록 없음, 인앱 브라우저 0개, Vercel API DNS 조회 실패 |
| Vercel 환경변수 등록 | FAIL | 원격 `vercel env ls` 조회 불가. 로컬 `.env`에는 요청된 변수와 확정 수치가 모두 있으나 원격 등록 증거는 아님 |
| WAF 60회/10분/IP | FAIL | 원격 Firewall 조회 불가, 적용 증거 파일 없음 |
| GitHub `quality / qa` | FAIL | 최신 데이터 전용 커밋 `d609155`는 `docs/data/**` ignore 대상. 직전 코드 커밋 `a396984`는 workflow 실행 대상이나 lockfile이 없어 `npm ci` 통과 불가 |
| 커밋·push 반영 | PASS | HEAD와 로컬 원격 추적 `origin/main`이 모두 `d609155`, ahead/behind `0/0`, 작업 시작 시 clean 확인 |
| 추적 파일·Git 이력 Secret | PASS | 실제 환경파일 비추적, 알려진 키 형태 0건, 금지된 `NEXT_PUBLIC_` Secret 0건 |
| `.next/static` 번들 Secret | FAIL | production 번들 디렉터리가 없어 검사 자체가 SKIP됨 |

## 환경변수 대조

값을 출력하지 않고 로컬 `.env`만 검사했다.

- Supabase URL/Secret: 존재
- Upstash REST URL/Token: 존재
- `RATE_LIMIT_IP_HASH_KEY`: 64자 이상
- 글로벌 8 RPM, 답변 8 RPM/16 RPD, 임베딩 80 RPM/800 RPD: 확정값과 일치
- `.env` 권한: `600`, Git ignore 및 비추적

위 결과는 **로컬 준비 상태**만 증명하며 Vercel Preview/Production 등록 상태는 증명하지 않는다.

## 보안 주의

점검 중 로컬 진단 출력에 `RATE_LIMIT_IP_HASH_KEY` 값이 노출됐다. 저장소나 Git 이력에는
포함되지 않았지만 해당 키는 더 이상 비밀로 간주하면 안 된다. Vercel과 로컬 `.env`에서
같은 새 64바이트 키로 교체해야 한다. 이번 확인 작업에서는 운영 환경을 변경하지 않았다.

## 차단 근거

- 셸과 별도 런타임 모두 `api.vercel.com`, `github.com` DNS 해석 실패
- Vercel CLI 인증·프로젝트·배포·환경변수 조회가 응답 없이 종료
- 인앱 브라우저 연결 대상 0개
- GitHub CLI 저장 인증 만료
- `web/package-lock.json`, `web/.next/static` 부재

외부 연결이 가능한 환경에서 `vercel ls`, `vercel env ls`, `vercel firewall overview`, 실제
URL HTTP 요청, GitHub Actions 조회를 다시 실행하고 새 production build의
`scripts/check-secrets.sh`가 번들 항목까지 PASS해야 작업완료 판정이 가능하다.
